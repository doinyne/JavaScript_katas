function validateHello(greetings) {
  if (greetings === "hello" ? true
       : "ciao" ? true
       : "salut" ? true
       : "hallo" ? true
       : "hola" ? true
       : "ahoj" ? true
       : "czesc" ? true;
       )

}

console.log(validateHello("no"))